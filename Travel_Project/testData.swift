//
//  testData.swift
//  Travel_Project
//
//  Created by Hoej, Christian R on 11/18/19.
//  Copyright © 2019 Hoej, Christian R. All rights reserved.
//

import UIKit

class testData: NSObject {
/*
private let urlSession = URLSession.shared
    
    let url = URL(string: "https://restcountries.eu/rest/v2/name/denmark?fullText=true")
 

    let dataTask2 = self.urlSession.dataTask(with: url! as URL) { (data, response, error) -> Void in
    guard let dataResponse = data,
              error == nil else {
              print(error?.localizedDescription ?? "Response Error")
              return }
        do{
            //here dataResponse received from a network request
            let jsonResponse = try JSONSerialization.jsonObject(with:
                                   dataResponse, options: [])
            print(jsonResponse) //Response result
         } catch let parsingError {
            print("Error", parsingError)
       }
    }
    dataTask2.resume()*/
    
}
